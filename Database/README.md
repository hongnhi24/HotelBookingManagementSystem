# Database Notes

The application expects a Microsoft SQL Server database named `HotelManagement`.

The original submission package did not contain a database creation script. Add a sanitized `.sql` schema-and-seed script to this folder before sharing the repository as a fully runnable demonstration.

Do not upload real customer information, personal data, passwords or production credentials.
